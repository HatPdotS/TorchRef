# Derived benchmark data — regenerated by run_benchmarks.py / plot_results.py.
# Keep the scripts (*.py, *.sbatch, *.sh, README.md) tracked; ignore outputs.
results_*/
output/

# SLURM job logs (the repo-root rule only matches a file named ".out", not "*.out")
*.out
*.err
